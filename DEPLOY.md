# 🚀 部署指南

本文档详细介绍如何将个人主页部署到 Vercel。

## 📋 前置要求

- GitHub 账号
- Vercel 账号（可用 GitHub 账号登录）
- Node.js 已安装（本地开发需要）

## 🌐 部署步骤

### 方法一：Vercel CLI 部署（最简单）

#### 1. 安装 Vercel CLI

```bash
npm install -g vercel
```

#### 2. 登录 Vercel

```bash
vercel login
```

这会打开浏览器进行授权。

#### 3. 部署项目

```bash
cd personal-homepage-v2-nextjs
vercel
```

按照提示操作：
- 设置项目名称
- 选择部署范围（个人或团队）
- 确认配置

完成！你的网站会得到一个 `.vercel.app` 的域名。

#### 4. 更新部署

每次修改代码后，只需再次运行：

```bash
vercel --prod
```

### 方法二：通过 Vercel 网站部署

#### 步骤 1：推送到 GitHub

```bash
# 初始化 git 仓库
git init

# 添加所有文件
git add .

# 提交
git commit -m "Initial commit"

# 推送到 GitHub（先在 GitHub 创建仓库）
git remote add origin https://github.com/yourusername/personal-homepage-v2-nextjs.git
git branch -M main
git push -u origin main
```

#### 步骤 2：在 Vercel 导入项目

1. 访问 [vercel.com](https://vercel.com)
2. 登录（推荐使用 GitHub 账号）
3. 点击 **"Add New..."** → **"Project"**
4. 找到你的仓库并点击 **"Import"**
5. 配置项目：
   - **Project Name**: `personal-homepage-v2-nextjs`（或自定义）
   - **Framework Preset**: Next.js（自动检测）
   - **Root Directory**: `./`（根目录）
   - **Build Command**: `npm run build`（自动填充）
   - **Output Directory**: 未设置（Next.js 自动处理）
6. 点击 **"Deploy"**

#### 步骤 3：等待部署

通常只需要 1-2 分钟，完成后你会看到：
- ✅ 部署成功的消息
- 🌐 你的网站 URL（如 `https://personal-homepage-v2-nextjs.vercel.app`）

### 方法三：自动部署（推荐）

配置好后，每次你推送代码到 GitHub，Vercel 会自动部署！

#### 配置方法：

1. 按照方法二的前两步操作
2. Vercel 会自动启用 GitHub 集成
3. 之后每次 `git push`，Vercel 都会自动部署

#### 示例工作流：

```bash
# 修改内容
vim data/profile.ts

# 提交更改
git add .
git commit -m "更新个人信息"
git push

# Vercel 自动开始部署！
```

## 🔧 配置自定义域名

### 在 Vercel 添加域名

1. 打开 Vercel 项目
2. 点击 **Settings** → **Domains**
3. 输入你的域名（如 `www.yourname.com`）
4. 按照提示配置 DNS 记录

### 配置 DNS

在你的域名提供商（如阿里云、腾讯云）添加：

```
类型: CNAME
主机记录: www
记录值: cname.vercel-dns.com
```

## 📊 查看部署状态

### 通过 Vercel Dashboard

1. 访问 [vercel.com/dashboard](https://vercel.com/dashboard)
2. 选择你的项目
3. 查看 **Deployments** 标签

### 通过 CLI

```bash
vercel ls
```

## 🔍 常见问题

### Q: 部署失败怎么办？

**A:** 检查以下几点：
1. 运行 `npm run build` 确保本地构建成功
2. 检查 Vercel 的部署日志（在 Dashboard 中）
3. 确保 `package.json` 配置正确

### Q: 如何查看部署日志？

**A:** 在 Vercel Dashboard：
1. 点击你的项目
2. 选择失败的部署
3. 查看 **Build Logs**

### Q: 部署后页面空白？

**A:** 可能原因：
1. 头像图片不存在（添加 `public/avatar.jpg`）
2. 检查浏览器控制台错误（F12）
3. 确认所有数据文件格式正确

### Q: 如何回滚到之前的版本？

**A:** 在 Vercel Dashboard：
1. 点击 **Deployments**
2. 找到之前的成功部署
3. 点击 **Promote to Production**

### Q: 部署需要多长时间？

**A:** 通常 1-2 分钟：
- Next.js 构建很快
- Vercel 自动优化
- 全球 CDN 加速

### Q: 可以用其他部署平台吗？

**A:** 可以！其他选择：
- **Netlify**: 类似 Vercel，也很优秀
- **GitHub Pages**: 免费，但功能较少
- **Cloudflare Pages**: 全球 CDN，速度快

## 🎯 部署检查清单

部署前确认：

- [ ] 已添加头像图片到 `public/avatar.jpg`
- [ ] 已修改 `data/profile.ts` 中的个人信息
- [ ] 已修改 `data/tools.ts` 中的工具列表
- [ ] 已修改 `data/articles.ts` 中的文章
- [ ] 本地运行 `npm run build` 成功
- [ ] 代码已推送到 GitHub
- [ ] Vercel 项目已导入

## 📈 性能优化

Vercel 自动为你优化：

- ✅ 代码分割
- ✅ 图片优化
- ✅ 静态生成 (SSG)
- ✅ CDN 加速
- ✅ 缓存策略

## 🎉 部署成功后

1. **分享你的网站**
   - 复制 Vercel 提供的 URL
   - 发给朋友或在社交媒体分享

2. **添加到 GitHub Profile**
   ```markdown
   [![Personal Website](https://img.shields.io/badge/Website-visit-blue)](https://your-site.vercel.app)
   ```

3. **持续更新**
   - 修改内容后 `git push`
   - Vercel 自动部署

## 💡 高级技巧

### 环境变量

如果需要使用环境变量：

1. 在 Vercel Dashboard → Settings → Environment Variables
2. 添加变量（如 `API_KEY`）
3. 在代码中通过 `process.env.API_KEY` 访问

### 分析和监控

Vercel 提供内置的分析：
- 访问量统计
- 性能指标
- 错误追踪

在 Dashboard → Analytics 查看。

---

**祝部署顺利！** 🎊

如有问题，查看 [Vercel 官方文档](https://vercel.com/docs)
