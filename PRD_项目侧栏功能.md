# PRD: 个人主页项目侧栏展示功能

**项目名称**: personal-homepage-v2-nextjs 项目侧栏
**版本**: v2.1.0
**文档日期**: 2025-02-04
**作者**: Claude & 用户协作

---

## 📋 目录

1. [项目背景](#1-项目背景)
2. [目标与范围](#2-目标与范围)
3. [功能需求](#3-功能需求)
4. [技术方案](#4-技术方案)
5. [UI/UX设计](#5-uiux设计)
6. [数据结构](#6-数据结构)
7. [实施计划](#7-实施计划)
8. [验收标准](#8-验收标准)
9. [风险与挑战](#9-风险与挑战)

---

## 1. 项目背景

### 1.1 现状分析

当前 `personal-homepage-v2-nextjs` 项目是一个现代化的个人主页，采用苹果官网设计风格，主要包含：
- Hero 区域（个人介绍）
- AI 工具推荐列表
- 文章展示列表

### 1.2 需求背景

用户开发了三个实用的小工具脚本，希望在个人主页的第一页添加侧栏来展示这些项目，以便：
- 展示个人技术能力和项目成果
- 为访客提供实用工具的入口
- 增强个人品牌影响力

### 1.3 展示的三个项目

| 项目名称 | 项目类型 | 核心功能 | 技术栈 |
|---------|---------|---------|--------|
| **微博黑名单管理工具** | Python 脚本 | 导出/批量拉黑微博黑名单 | Selenium, JSON/CSV/TXT |
| **B站评论区热词分析工具** | Python 脚本 | 评论抓取、关键词分析、情感分析 | Requests, Jieba, SnowNLP |
| **B站UP主和热门榜单监控** | Python 脚本 | UP主更新监控、热门榜单变化检测 | Requests, 飞书机器人, SMTP |

---

## 2. 目标与范围

### 2.1 主要目标

1. ✅ **展示个人项目**：在首页侧栏展示三个实用工具项目
2. ✅ **提升用户体验**：让访客快速了解作者的技能和作品
3. ✅ **保持设计一致性**：侧栏设计需符合现有的苹果风格设计语言
4. ✅ **易于维护**：通过数据文件管理项目信息，无需修改代码

### 2.2 范围界定

**包含**：
- 在首页添加左侧/右侧边栏（响应式设计）
- 展示三个项目卡片（图标、标题、描述、链接、标签）
- 支持悬停动画效果
- 响应式布局（移动端自动调整）

**不包含**：
- 项目详情页（点击后在当前页/新标签页打开外部链接）
- 后台管理系统
- 用户交互功能（评论、点赞等）

---

## 3. 功能需求

### 3.1 核心功能

#### F1: 侧栏布局
- **位置**: 右侧边栏（桌面端）
- **宽度**: 300-350px
- **响应式**:
  - 桌面端（>1024px）：右侧固定侧栏
  - 平板端（768-1024px）：可折叠侧栏
  - 移动端（<768px）：移动到内容下方或作为独立区块

#### F2: 项目卡片展示
每个项目卡片包含：
- 🎨 **项目图标/封面**
- 📝 **项目标题**
- 📄 **简短描述**（1-2句话）
- 🏷️ **技术标签**（2-4个）
- 🔗 **项目链接**（GitHub/GitLab/演示地址）
- ✨ **悬停效果**

#### F3: 交互效果
- 卡片悬停：轻微放大、阴影增强
- 渐入动画：滚动到可视区域时淡入
- 点击反馈：按下效果（模拟苹果按钮交互）

### 3.2 非功能需求

| 需求项 | 说明 |
|--------|------|
| **性能** | 侧栏加载时间 < 500ms |
| **兼容性** | Chrome, Firefox, Safari, Edge 最新版本 |
| **响应式** | 支持手机、平板、桌面三种设备 |
| **可维护性** | 通过 data/projects.ts 管理项目数据 |
| **设计一致性** | 遵循现有毛玻璃、渐变、动画风格 |

---

## 4. 技术方案

### 4.1 技术栈

```
Next.js 15 (App Router)
├── React 19
├── TypeScript
├── Tailwind CSS
└── 现有项目结构
```

### 4.2 文件结构

```
personal-homepage-v2-nextjs/
├── app/
│   ├── page.tsx                 # 修改：添加侧栏布局
│   ├── layout.tsx               # 保持不变
│   └── globals.css              # 修改：添加侧栏样式
├── components/                   # 新增
│   ├── Sidebar.tsx              # 侧栏容器组件
│   ├── ProjectCard.tsx          # 项目卡片组件
│   └── SectionTitle.tsx         # 区块标题组件
├── data/
│   ├── profile.ts               # 保持不变
│   ├── tools.ts                 # 保持不变
│   ├── articles.ts              # 保持不变
│   └── projects.ts              # 新增：项目数据
└── types/
    └── project.ts               # 新增：项目类型定义
```

### 4.3 组件设计

#### Component 1: Sidebar
```typescript
// components/Sidebar.tsx
interface SidebarProps {
  projects: Project[];
  className?: string;
}

功能：
- 渲染侧栏容器
- 毛玻璃效果背景
- 响应式布局切换
```

#### Component 2: ProjectCard
```typescript
// components/ProjectCard.tsx
interface ProjectCardProps {
  project: Project;
  index: number; // 用于延迟动画
}

功能：
- 渲染单个项目卡片
- 悬停动画效果
- 渐入动画
```

### 4.4 样式方案

**毛玻璃效果**：
```css
backdrop-filter: blur(20px);
background: rgba(255, 255, 255, 0.1);
border: 1px solid rgba(255, 255, 255, 0.2);
```

**渐入动画**：
```typescript
// 使用现有的 Intersection Observer
const observer = new IntersectionObserver((entries) => {
  entries.forEach(entry => {
    if (entry.isIntersecting) {
      entry.target.classList.add('fade-in');
    }
  });
});
```

---

## 5. UI/UX设计

### 5.1 设计原则

1. **苹果风格一致性**：延续现有的毛玻璃、渐变、流畅动画
2. **简洁优先**：卡片信息层次清晰，不拥挤
3. **交互反馈**：悬停、点击有明确的视觉反馈

### 5.2 视觉设计

#### 侧栏整体样式
- **背景**: 毛玻璃效果（透明度 10%）
- **边框**: 1px 细线，白色半透明
- **圆角**: 16px（与现有设计一致）
- **内边距**: 24px

#### 项目卡片样式
```
┌─────────────────────────┐
│  🎨 图标/封面            │
│                         │
│  项目标题                │
│  简短描述...             │
│                         │
│  [Python] [Selenium]    │
│  [自动化]               │
│                         │
│  查看详情 →              │
└─────────────────────────┘
```

**颜色方案**：
- 标题：渐变色（蓝红渐变，与 Hero 标题一致）
- 描述：浅灰色（可读性优先）
- 标签：半透明背景，白色文字
- 链接：蓝色，悬停时亮度提升

#### 响应式断点

| 屏幕宽度 | 布局方式 |
|---------|---------|
| < 768px | 移动端：卡片在内容下方，单列布局 |
| 768-1024px | 平板端：侧栏可折叠/展开 |
| > 1024px | 桌面端：右侧固定侧栏 |

### 5.3 交互动画

#### 悬停效果
```css
.project-card:hover {
  transform: translateY(-4px) scale(1.02);
  box-shadow: 0 12px 24px rgba(0, 0, 0, 0.15);
}

.project-card {
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
}
```

#### 渐入动画
```css
@keyframes fadeInUp {
  from {
    opacity: 0;
    transform: translateY(20px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

.fade-in {
  animation: fadeInUp 0.6s ease-out forwards;
}
```

---

## 6. 数据结构

### 6.1 类型定义

```typescript
// types/project.ts
export interface Project {
  id: string;              // 唯一标识
  title: string;           // 项目标题
  description: string;     // 简短描述（1-2句话）
  icon?: string;           // 图标 URL（可选）
  cover?: string;          // 封面图 URL（可选）
  tags: string[];          // 技术标签
  links: {
    github?: string;       // GitHub 链接
    demo?: string;         // 演示链接
    docs?: string;         // 文档链接
  };
  featured?: boolean;      // 是否精选项目
  status?: 'active' | 'maintenance' | 'archived'; // 项目状态
}
```

### 6.2 数据文件

```typescript
// data/projects.ts
import { Project } from '@/types/project';

export const projects: Project[] = [
  {
    id: 'weibo-blacklist-manager',
    title: '微博黑名单管理工具',
    description: '强大的微博黑名单管理工具，支持导出黑名单、一键拉黑和批量拉黑功能，帮助你轻松管理社交环境。',
    tags: ['Python', 'Selenium', '自动化', '社交媒体'],
    links: {
      github: 'https://github.com/yourusername/weibo-blacklist-manager',
      docs: 'https://github.com/yourusername/weibo-blacklist-manager/blob/main/README.md'
    },
    featured: true,
    status: 'active'
  },
  {
    id: 'bilibili-comment-analyzer',
    title: 'B站评论区热词分析工具',
    description: '自动抓取B站视频评论，进行关键词统计、情感分析，并生成可视化HTML报告。',
    tags: ['Python', 'Jieba', 'SnowNLP', '数据分析', 'NLP'],
    links: {
      github: 'https://github.com/yourusername/bilibili-comment-analyzer',
      demo: './demo/bilibili-analysis.html'
    },
    featured: true,
    status: 'active'
  },
  {
    id: 'bilibili-monitor',
    title: 'B站UP主和热门榜单监控',
    description: '自动监控你关注的B站UP主和热门榜单，通过飞书机器人和邮件第一时间接收更新通知。',
    tags: ['Python', 'Requests', '飞书机器人', '监控'],
    links: {
      github: 'https://github.com/yourusername/bilibili-monitor',
      docs: '#'
    },
    featured: true,
    status: 'active'
  }
];
```

---

## 7. 实施计划

### 7.1 开发阶段

#### 阶段 1: 数据准备（预计 30 分钟）
- [ ] 创建 `types/project.ts` 类型定义文件
- [ ] 创建 `data/projects.ts` 数据文件
- [ ] 填充三个项目的详细信息

#### 阶段 2: 组件开发（预计 2 小时）
- [ ] 创建 `components/Sidebar.tsx` 侧栏容器
- [ ] 创建 `components/ProjectCard.tsx` 项目卡片
- [ ] 创建 `components/SectionTitle.tsx` 区块标题（可选）

#### 阶段 3: 页面集成（预计 1 小时）
- [ ] 修改 `app/page.tsx`，添加侧栏布局
- [ ] 调整主内容区域宽度，为侧栏留出空间
- [ ] 实现响应式布局切换

#### 阶段 4: 样式优化（预计 1 小时）
- [ ] 添加毛玻璃效果样式
- [ ] 实现悬停动画
- [ ] 实现渐入动画
- [ ] 响应式断点调试

#### 阶段 5: 测试与优化（预计 1 小时）
- [ ] 跨浏览器测试
- [ ] 移动端/平板/桌面端测试
- [ ] 性能优化
- [ ] 代码重构和优化

**总计预计时间**: 5-6 小时

### 7.2 实施顺序

```
1. 数据定义 → 2. 组件开发 → 3. 页面集成 → 4. 样式优化 → 5. 测试
```

---

## 8. 验收标准

### 8.1 功能验收

| 功能 | 验收标准 | 优先级 |
|-----|---------|--------|
| **侧栏显示** | 在桌面端右侧显示侧栏，包含三个项目卡片 | P0 |
| **项目卡片** | 卡片包含标题、描述、标签、链接，信息完整 | P0 |
| **响应式布局** | 移动端/平板/桌面端布局正确切换 | P0 |
| **悬停效果** | 鼠标悬停时卡片有放大和阴影效果 | P1 |
| **渐入动画** | 滚动到可视区域时卡片淡入显示 | P1 |
| **链接跳转** | 点击卡片或按钮正确跳转到对应链接 | P0 |
| **设计一致性** | 侧栏风格与整体页面风格一致 | P0 |

### 8.2 性能验收

- ✅ 页面加载时间 < 2s
- ✅ 侧栏渲染时间 < 500ms
- ✅ 无明显卡顿或掉帧

### 8.3 兼容性验收

| 浏览器 | 版本要求 |
|--------|---------|
| Chrome | 最新版 + 前2个版本 |
| Safari | 最新版 + 前2个版本 |
| Firefox | 最新版 + 前2个版本 |
| Edge | 最新版 + 前2个版本 |

### 8.4 移动端验收

- ✅ iOS Safari 13+
- ✅ Android Chrome 80+
- ✅ 布局在小屏幕上可读可用
- ✅ 触摸交互流畅

---

## 9. 风险与挑战

### 9.1 潜在风险

| 风险 | 影响 | 概率 | 缓解措施 |
|-----|------|------|---------|
| **响应式布局复杂度** | 中 | 中 | 使用 Tailwind 的响应式工具类，充分测试多个断点 |
| **动画性能问题** | 低 | 低 | 使用 CSS transform 和 opacity，避免触发重排 |
| **设计不一致** | 中 | 中 | 参考现有组件的样式，保持一致的设计系统 |
| **数据管理复杂性** | 低 | 低 | 使用 TypeScript 确保类型安全，数据集中管理 |

### 9.2 技术挑战

#### Challenge 1: 响应式布局切换
**问题**: 在不同屏幕尺寸下，侧栏布局需要平滑切换

**解决方案**:
```typescript
// 使用 Tailwind 响应式类
<div className="
  hidden lg:block          // 移动端隐藏
  lg:fixed lg:right-8      // 桌面端固定右侧
  lg:w-80                  // 桌面端宽度 320px
">
```

#### Challenge 2: 动画延迟实现
**问题**: 多个卡片需要依次渐入，营造层次感

**解决方案**:
```typescript
// 给每个卡片添加延迟
<div
  style={{
    animationDelay: `${index * 100}ms`
  }}
>
```

### 9.3 后续扩展

#### 未来可能的功能
- [ ] 添加筛选功能（按技术栈筛选）
- [ ] 添加搜索功能
- [ ] 添加项目排序（按日期、热度等）
- [ ] 添加更多项目展示
- [ ] 添加项目统计信息（Star数、Fork数等）

---

## 10. 附录

### 10.1 参考资料

- [Tailwind CSS 响应式设计](https://tailwindcss.com/docs/responsive-design)
- [Next.js 15 App Router](https://nextjs.org/docs/app)
- [Intersection Observer API](https://developer.mozilla.org/en-US/docs/Web/API/Intersection_Observer_API)

### 10.2 设计灵感

- Apple 官网产品展示区
- Vercel 组件库展示
- Linear App 产品页

---

## 📝 变更记录

| 版本 | 日期 | 变更内容 | 作者 |
|-----|------|---------|------|
| v1.0.0 | 2025-02-04 | 初始版本，完成 PRD 编写 | Claude & 用户 |

---

<div align="center">

**[⬆ 回到顶部](#prd-个人主页项目侧栏展示功能)**

Made with ❤️ for personal-homepage-v2-nextjs

</div>
